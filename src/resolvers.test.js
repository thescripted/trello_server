// Testing Necessary Features & Implementations of my Resolvers. Utilizies Jest.
// This test is incomplete, and only targets the most important queries & mutations.

beforeAll(async () => {
  // Open connection to database. This test occurs on a separate database.
})

afterAll(async () => {
  // Close connection to database
})
